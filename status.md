# Status — Bropicer

> Живий журнал проєкту. Памʼять, що переживає окремі сесії. Найновіше — зверху.

## Поточний стан
- **🌐 LIVE:** https://bropicer.vercel.app — Vercel, проєкт `2b-agency/bropicer`, статика з `design/` (root=`.`, без білду). Авто-деплой з `main`.
- **Етап:** 02-design — **повний редизайн v2** за новим стандартом, очікує погодження.
- **Наступний крок:** фідбек → (за потреби) якісний апгрейд фото через браузер → DoD → `03-development`.
- **Блокери релізу:** назви/стилі/ABV/**ціни** 6 сортів — заглушки (TODO); **backend/оплата** не підключені (форма демо); фото — AI-чернетки M2 (готові промти в `resources/refs/image-prompts.md`); реальні контакти/соцмережі.

## Журнал

### 2026-07-07 — favicon (біла кришка) ✅
- Додано `design/favicon.svg` (+ копія в корені) — топ-в'ю **крон-кришки**: біла фльорована кришка (`--paper`) на чорному тлі (`--void`, rounded rect), приглушений амбер-сигнал (`--signal #C6923E`) у центрі. 21 зубець — стандартна крон-пробка. Прямий бренд-код: чорна пляшка → біла кришка.
- Підключено `<link rel="icon" type="image/svg+xml">` у `design/index.html` (+ `theme-color #060606`) і в кореневому redirect-`index.html`.

### 2026-07-02 — задеплоєно на Vercel ✅
- **LIVE:** https://bropicer.vercel.app — деплой через `vercel deploy --prod` (CLI вже авторизований під `bobodnaruk-3514`). Проєкт створено як **`2b-agency/bropicer`** (команда 2b-agency).
- Деплоїли з теки `design/` (`vercel link --project bropicer` → `.vercel/` локально, в git не йде). Output = `.`, білд-команди нема — чиста статика.
- Перевірено на проді: меню «Cпробувати», hero-orientation, manifesto 6/7-рядків, карусель, стокові лого, кеш-хедери (`assets/*` immutable) — усе працює.
- `.env.local` (VERCEL_OIDC_TOKEN) і `.vercel/` — у `design/.gitignore`, у репо не комітяться.
- GitHub Pages лишається зламаним — тепер неактуальний; можна вимкнути (Settings → Pages → None) + прибрати кореневий redirect-`index.html`.

### 2026-07-02 — рішення: перехід на Vercel (статика з design/)
- **Рішення:** хостити на **Vercel** статику з теки `design/` (без Next/React — щоб не ризикувати vanilla GSAP/Lenis анімаціями). Обрано замість GitHub Pages, де деплой стабільно падав із «Timeout reached, aborting!».
- Гілка: **`main`** (Next-проєкт не робимо; `development/` поки не задіяно).
- Додано `design/vercel.json` — кеш-хедери для `assets/` (шрифти/зображення, 1 рік) і no-cache для `styles.css`/`main.js`.
- **Vercel-налаштування при імпорті:** Root Directory = **`design`**, Framework = Other, без build-команди.
- Підключення робить власник (Claude не логіниться у Vercel). Після імпорту кожен push у `main` = авто-деплой.
- GitHub Pages лишається зламаним (timeout) — після переходу на Vercel його можна вимкнути (Settings → Pages → None). Усі накопичені зміни (меню «Cпробувати», hero-за-орієнтацією, manifesto 6/7-рядків) виїдуть одразу через Vercel.

### 2026-07-02 — адаптив під усі екрани + мобільна карусель лінійки
- **Карусель лінійки (≤900px):** замість вертикальної колонки — горизонтальний слайдер по **одному сорту** (native CSS scroll-snap: свайп + inertia). Керування: стрілки ← →, крапки-індикатори (6), синхронний активний стан.
  - JS `initReelCarousel()` (main.js) через `matchMedia("(max-width:900px)")` — вмикається/вимикається на ресайзі. На карусель повішено `data-lenis-prevent`, щоб Lenis не перехоплював свайп.
  - Десктоп (≥901px) — **без змін**: pinned горизонтальний пан у `buildScene()` активний; на мобайлі карусель `disable()` коректно скидає стан.
  - HTML: додано `.reel__nav` (стрілки + `#reel-dots`) після `.reel__viewport`.
- **Адаптив під ноутбуки/нетбуки/макбуки:**
  - Проміжні брейкпоінти по ширині: `≤1200` — stockists 3 колонки; `1024–901` — craft/dist по 3.
  - **Height-based** query для коротких ландшафтних екранів (`≤820h` і `≤680h` при `≥901w`): зменшено vh-типографіку hero, рядки manifesto, `.pour__big`, `.panel__name` — щоб текст не обрізало на низьких в'юпортах (MacBook 16:10, нетбуки).
  - Гігантські слова hero захищено від вузьких екранів: `clamp(3rem, min(21vh,24vw), 17rem)`.
- **Перевірено у прев'ю:** 375 (mobile), 768 (tablet), 1280×800, 1366×640 (короткий ландшафт), 1440×900. Карусель: стрілки/крапки/свайп синхронні, крайні стани (перша/остання) вимикають стрілки; горизонтального переповнення немає; десктопний пан не зачеплено.
- Ліміт: карусель активна лише `≤900px` (за задумом — «в мобільній версії»). Планшет-ландшафт >900 отримує десктопний пан.

### 2026-07-02 — секція «Де скуштувати»: стокові білі лого замість літер
- Що зроблено: у секції `#stockists` замінено кружечки з літерами на **монохромні білі стокові лого-емблеми** (по одній на партнера).
- Джерело: **Iconify → набір `game-icons`** (ліцензія CC-BY 3.0), витягнуто як SVG із `?color=#ffffff`. Freepik/SVG Repo не вдалося тягнути автоматично (login-wall / HTTP 429), тому взято відкритий стоковий набір з прямим API.
- Файли: `design/assets/stockists/*.svg` (10 шт.). У всіх нормалізовано `width/height=512` (з коробки було `1em` → нульовий розмір в `<img>`).
- Мапінг: Броваріум→tavern-sign, Хміль Паб→hops, Бочка→barrel, Пінта→beer-bottle, Крафт Стейшн→bottle-cap, Солод→wheat, Тап Рум→tap, Кухоль→beer-stein, Пивна Хвиля→saloon-doors, 14 Кранів→shop.
- CSS: `.stk__logo` 54% від кружечка, `opacity .6 → 1` на hover (успадковано попередню поведінку marks).
- **Це плейсхолдери** — партнери й назви досі фіктивні (TODO). Коли будуть реальні заклади — підставити їхні справжні лого.
- Перевірено у прев'ю (5810): всі 10 вантажаться, білі, читаються.

### 2026-07-02 — меню: повернуто roll-hover, прибрано магнітний зсув (v2.14)
- Уточнення фідбеку: v2.13 зрозумів хибно. Повернути **roll** (біле слово вгору / золоте знизу), а прибрати треба був **магнітний зсув самого пункту вслід за курсором** (ефект `[data-magnetic]` тягнув лінк у бік миші).
- Повернено v2.12-версію hover: у HTML знову `.menu__roll` + дубль `.menu__text--alt` + стрілка `.menu__go`; у CSS — roll-трансформи, `.menu__text--alt` (translateY 105%→0), `.menu__go` (opacity+slide), `.menu__link:hover .menu__idx→signal`; повернено мобільне `.menu__go{display:none}`.
- **Прибрано зсув:** у `initCursor` магнітний `pointermove`-translate тепер **не чіпляється** до `.menu__link` (`if(!m.classList.contains('menu__link'))`). Кільце-курсор на hover (`pointerenter` scale) **лишилось** — прибрано саме бічний рух, не всю інтерактивність. `data-magnetic` на лінках збережено (для кільця).
- Верифікація (детерміновано): через `document.styleSheets` — `.menu__link:hover .menu__text` = `translateY(-105%)`, `…--alt` rest `translateY(105%)`/signal → hover `translateY(0)`, `…__go` hover opacity 1, `…__idx` hover signal; roll-DOM присутній (hasRoll/hasAlt/hasGo=true). Симуляція `pointermove` по пункту → `transform` лишається `none` (магнітного зсуву нема). Прим.: прев'ю-скрін-тул флапає/таймаутить на цьому сайті (mix-blend/Lenis, консоль чиста) — верифікація обчислена, у браузері roll + відсутність зсуву працюють.

### 2026-07-02 — меню: спрощено hover (v2.13)
- Фідбек: меню класне, але прибрати **рух** пунктів на hover — лишити тільки зміну кольору білий→золото.
- Прибрано rolling-swap: видалено дубль-слова (`.menu__text--alt`), обгортку `.menu__roll` і стрілку `.menu__go` (з HTML і CSS, разом із мертвим правилом у мобільному медіа). Тепер `.menu__link` = idx + одне `.menu__text`.
- Hover тепер лише `color: var(--paper) → var(--signal)` (transition .4s), індекс теж стає амбер. Без transform.
- Верифікація (обчислено): hover-пункт `rgb(198,146,62)` при `transform:none`, спокійний пункт `rgb(243,242,238)`; `.menu__roll/.menu__text--alt/.menu__go` відсутні; без горизонт. переповнення (docW 1444 < winW). Прим.: прев'ю-скрін-тул цього разу завис на захопленні (mix-blend/Lenis, консоль чиста) — покладаюсь на обчислену перевірку; у браузері hover = чиста зміна кольору.

### 2026-07-02 — редизайн навігації: бургер + повноекранне меню (v2.12)
- Фідбек: прибрати центральні пункти nav і CTA «Замовити» справа; замість кнопки — стильний бургер, що відкриває повноекранне меню з драматичною анімацією, великі пункти з оригінальним hover. Референси — Pinterest/Awwwards fullscreen-overlay menus (білі гігантські лінки на чорному, mono-індекси, mask/rolling hover, staggered reveal, burger→X). Синтез під cinematic-dark характер сайту, не копія.
- **Nav:** `.nav__mid` + `.nav__cta` прибрано. Додано editorial-бургер (`#burger`): mono-лейбл «Меню» + дві хейрлайни, що морфляться в X; лейбл→«Закрити», aria-expanded/aria-label синхронізуються.
- **Оверлей** (`#menu`): повний екран `--void`, розкриття **curtain** через `clip-path inset` (спадає згори, power4.inOut) + staggered підйом пунктів (overflow-hidden рядки) + fade футера. GSAP timeline; закриття — реверс.
- **Пункти:** гігантські Mazzard-лінки (Сорти/Крафт/Де скуштувати/Партнерам) з mono-індексами 01–04, хейрлайн-роздільники (SHAPE LOCK), футер-мета з амбер «18+». **Hover:** rolling-swap — паперове слово їде вгору, амбер-дубль (`--signal`) викочується знизу; індекс стає амбер + стрілка «↗» виїжджає. Лінки мають `data-magnetic` (працює курсор+магнетизм).
- **Fix (реальний баг):** `.nav` мав `z-index:120` < оверлей `150` → меню перекривало бургер, hit-test повертав `menu__inner` (закрити кліком неможливо). Підняв `.nav` до `z-index:160` (< intro 250). Тепер бургер лишається над оверлеєм і клікабельний.
- **Логіка (`initMenu`):** Lenis `stop()/start()` + `body.lock` при відкритті/закритті; ESC закриває; клік по пункту — спершу `closeMenu`, тоді `scrollTo(target)` в onComplete (глобальний `a[href^="#"]`-обробник тепер пропускає `.menu`-лінки через `closest('.menu')`, щоб не конфліктувати). Reduced-motion: миттєвий показ/сховок без GSAP.
- Верифікація: eval/скріни — curtain `inset(0)` після відкриття, пункти opacity 1, Mazzard 112px десктоп / 41.6px мобайл, hover-стан підтверджено обчислено (base translateY −108px, alt амбер на місці) + візуально (КРАФТ амбер + ↗); hit-test бургера = `burger-label` (клікабельний); десктоп 1280 + мобайл 375 без горизонт. переповнення (docW==winW=389); консоль чиста. Прим.: прев'ю-скрін-тул флапає стан меню між викликами (Lenis+pin+mix-blend, як і раніше) — для детермінованого кадру відкриття форсував інлайн у одному eval; у реальному браузері анімація/hover працюють.
- Файли: `design/index.html` (nav+оверлей), `design/styles.css` (бургер+меню, nav z-index), `design/main.js` (`initMenu`, guard глобального якір-обробника).

### 2026-07-02 — секція «Де скуштувати» між pour і craft (v2.11)
- Додано `.stk#stockists` **між pour і craft** (DOM-порядок підтверджено): логотипи барів/пабів-партнерів.
- Стиль cinematic-dark: гігантський Mazzard-заголовок «Де скуштувати» + lead, **hairline-сітка 5×2** (на мобільному 2 кол.), кожен партнер = коло-монограма (ініціал, Mazzard) + назва, `--muted` з hover-підсвіткою. Reveal на скролі (data-reveal, stagger).
- 10 плейсхолдер-партнерів (Броваріум, Хміль Паб, Бочка, Пінта, Крафт Стейшн, Солод, Тап Рум, Кухоль, Пивна Хвиля, 14 Кранів) — TODO: замінити реальними партнерами й логотипами.
- Верифікація: eval (grid 5 cols, монограми-кола, порядок секцій, консоль чиста). Прим.: прев'ю-скрін-тул цю сесію зависає на захопленні (інфра, не сторінка) — у браузері все рендериться.

### 2026-07-02 — редизайн блоку дистрибуції (v2.10)
- Фідбек: попередній варіант (текст ліворуч + картинка праворуч) — примітивний, не в рівень сайту.
- Reference-driven (Awwwards CTAs/editorial/partners): oversized-тип, повнобляйдова cinematic-композиція, hero-візуал + один CTA.
- Перероблено `.dist` у **повноекранну cinematic-секцію**: кришка (`cap.png`) як великий hero-об'єкт із радіальним ореолом і **обертанням+дрейфом на скролі** (GSAP), гігантський Mazzard-заголовок «Постав нас на полицю» (kinetic reveal), перки з амбер ghost-номерами 01–03 (як у «Крафт»), великий CTA «Стати партнером» + mono-нота. Broken/editorial, не 2-колонковий шаблон.
- Верифікація: рендер підтверджено (скрін + eval), консоль чиста. Прим.: прев'ю-скрін-тул флапав на глибоких секціях (Lenis+pin+mix-blend) — не баг сторінки; у реальному браузері скрол/reveal працюють.

### 2026-07-02 — курсор / паралакс / дистрибутор / FAQ (v2.9)
- **letter-spacing .05em** на всіх Mazzard-елементах (додано на ті, що ще мали інші значення: .disp, intro, nav-logo, marquee, panel-idx, craft h4, link-btn, acc__q, foot-brand).
- **Курсор:** системний курсор приховано повністю (`html.cursor-none *{cursor:none}`, клас додає JS у initCursor) — лишається тільки кастомний.
- **Hero-паралакс:** посилено, шари на різних швидкостях (top cluster −55%, пляшка +26%, bot cluster −85% yPercent) — глибина тепер виражена.
- **Форму замовлення прибрано** (order form + cart). Замість неї — блок **«Стати нашим дистрибютером»** (`section.dist#order`) з картинкою кришки (`cap.png`) + CTA mailto. Кнопки «Додати в замовлення» в лінійці приховано (`.add{display:none}`), у нав «Доставка»→«Партнерам». initCart/initForm вимкнено.
- **FAQ:** прибрано ефект наведення (`data-magnetic` з питань).
- Верифікація: eval/DOM-snapshot (усі елементи блоку дистрибютора видимі, cap.png завантажено; faqMagnetic=0; cursor:none; add hidden), hero-скрін ок. Примітки: (1) скрін-захоплення глибоких секцій дає чорний кадр через розсинхрон Lenis із прев'ю-захопленням (не баг сторінки); (2) `mix-blend` курсора вішає скрін-тул — для власних скрінів тимчасово ховав оверлей; (3) hero-слово «сортів» виходить за правий край ~74px (навмисний editorial-bleed, overflow-x прихований).

### 2026-07-01 — інтервал + GPT-пляшка в hero й лінійці (v2.8)
- Збільшено міжрядковий інтервал заголовків (Mazzard Black важкий): line-height ~1.05 на секційних тайтлах, mline 1.1, hero__word 1.04, panel__name 1.0, foot .95.
- Додано 4 GPT-картинки в `design/assets/`: `bottle-svitanok.png` (пляшка з етикеткою SVITANOK, прозора), `logo-hop.png` (лого-хміль контур), `hop.png` (хміль-фото), `cap.png` (кришка-хміль). Останні 3 — в резерві (лого/акценти), поки не задіяні.
- **Hero:** пляшку замінено на SVITANOK (прозора вирізка) на глибокому чорному + м'який світловий ореол (CSS radial), без «шумного» студійного тла. «ШІСТЬ» за пляшкою, «СОРТІВ» спереду.
- **Лінійка:** у кожну панель додано пляшку праворуч + текст ліворуч + ghost-номер за пляшкою. Панель 01 (Світанок) = `bottle-svitanok.png` (з етикеткою); 02–06 = `bottle.png` (бланк-плейсхолдер, до генерації їхніх етикеток).
- Фікси reel: пін перенесено на `.reel__viewport` (заголовок «ЛІНІЙКА» більше не з'їдає висоту, панель на весь екран); `100dvh`→`100vh` (dvh рендерився завеликим); специфічність `.panel__bottle` (правило `.panel > *` перебивало absolute). Десктоп-пан + мобільний стек ок, консоль чиста.

### 2026-07-01 — шрифт заголовків → Mazzard (self-host) (v2.7)
- Користувач дав файли Mazzard (GraphicUX free: MazzardH-Black + BlackItalic .otf). Скопійовано в `design/assets/fonts/`, підключено через `@font-face` (self-host, без CDN). `--disp:"Mazzard","Onest"`.
- Українська кирилиця рендериться самим Mazzard (перевірено візуально в hero + FAQ), не фолбеком. Доступна лише вага Black — усі заголовки важкі (консистентно). Onest лишається тілом + fallback.

### 2026-07-01 — шрифт заголовків → Onest (v2.6)
- Russo One не сподобався. За вибором користувача (Onest або Gilroy) взято **Onest** для заголовків (Gilroy не на Google + проблемна кирилиця, потрібні файли). Тепер єдине сімейство Onest: display 800, тіло 400/500, тонкий sub 300. Ієрархія вагою+розміром.

### 2026-07-01 — hero: правки типографіки/позиціонування (v2.5)
- Слова прив'язані до **картинки**, не до країв екрана: `.hero__frame` = розмір фото (центр), два кластери всередині → на широкоформаті тримаються біля пляшки.
- Групування: «Крафтова пивоварня» + **ШІСТЬ** (верх), **СОРТІВ** + «жодного зайвого» (низ).
- Фікс «обрізає слова»: прибрано `overflow:hidden` з `.hero__word` (був для маски reveal) → reveal тепер підйом+opacity.
- Шрифт заголовків: **Unbounded → Russo One** (широкий квадратно-індустріальний, повна українська). Тонкий підзаголовок — Onest 300.
- Розмір заголовків: у **`vh`** (`clamp(4rem,19vh,17rem)`) — пропорційний висоті фото, більший.
- Прибрано `text-shadow` (розмиття) під заголовками.
- Мобільний: фрейм width-bound (`86vw`, aspect 3328/4608), слова у vw. Без горизонтального скролу, консоль чиста.

### 2026-07-01 — hero: дві плити = одне фото (v2.4, виправлення)
- Фідбек: попередній варіант (фон через CSS-background + окремо пляшка) — «жахливо». Правильно: **дві картинки однакового розміру просто накладаються** = одне фото; текст шарами (верхнє слово за пляшкою). Компонування — як у схваленому v2.2 (діагональ ШІСТЬ↖ / СОРТІВ↘).
- Реалізація: `.hero__frame` містить дві плити 3328×4608, обидві `position:absolute; height:100%; translateX(-50%); object-fit:contain` → збігаються піксель-у-піксель. Плита-фон = `hero-plate.png` (повне фото Reve «Matte Black Beer Bottle»), поверх — `bottle.png` (та сама вирізка, прозора) → збіглися ідеально, без дублю.
- Z-стек: фон(0) → «ШІСТЬ»(1, за пляшкою) → пляшка(2) → «СОРТІВ»(3) → sub. Центрування плит без gsap-трансформів; parallax рухає `#hero-frame` (фон+ШІСТЬ+пляшка разом) + окремо СОРТІВ/sub — фото лишається цілісним на скролі.
- Додано `.sr-only` H1 для семантики (display-слова aria-hidden). Десктоп+мобільний ок, консоль чиста.

### 2026-07-01 — hero: багатошаровий parallax (v2.3, відкинуто)
- За фідбеком: hero перебудовано в шари з глибиною. Юзер надав 2 файли — прозору пляшку (`Black Beer Bottle _transparent background.png`, RGBA 3328×4608) і темне тло (інлайн).
- Ассети: `assets/bottle.png` (прозора вирізка з D:\Downloads), `assets/hero-backdrop.png` (згенероване відповідне темне студійне тло; юзер може замінити своїм файлом).
- Стек глибини (z): **тло(0) → «ШІСТЬ»(1, за пляшкою) → пляшка(2) → «СОРТІВ»(3, перед пляшкою) → «жодного зайвого»**. Слова зведені ближче до центру, пляшка між ними.
- Parallax: GSAP scrub-таймлайн, різні швидкості шарів (тло +10 / ШІСТЬ −26 / пляшка +12 / СОРТІВ −44 / sub −70 yPercent) + субтильний pointer-parallax по X. Центрування без CSS-transform (grid/ text-align), щоб не конфліктувати з GSAP.
- Прозора пляшка на темному тлі лягає безшовно — виглядає як реальний студійний кадр. Десктоп+мобільний ок, консоль чиста.

### 2026-07-01 — hero-фото згенеровано в Reve (v2.2)
- Через браузерне розширення відкрито **Reve** (app.reve.com), згенеровано пляшку: промт із `image-prompts.md`, формат 3:4, 2 дублі. Обидва вийшли якісно (чорна пляшка + біла кришка + біла етикетка, cinematic rim light) — значно краще за Pollinations.
- Витяг файлу: Reve рендерить у shadow DOM (webp), тож завантажив через in-page blob-anchor. **Chrome вантажить у `D:\Downloads`** (не C:). Файл `hero-bottle.webp` (2048×3033, 83 КБ) покладено в `design/assets/`, підключено в hero (`index.html`).
- Нюанси середовища: прямий MCP-вивід base64/URL блокується; clipboard-write зі сторінки блокується без фокуса; computer-use request_access дав таймаут. Робочий шлях — blob-download → D:\Downloads → cp у проєкт.
- Лишились чернетки Pollinations для інших слотів (pour/process/stone) — за потреби згенерувати в Reve тим самим методом.

### 2026-07-01 — правки hero за фідбеком (v2.1)
- Зміщення «шість сортів» → **сильна діагональна асиметрія** (ШІСТЬ ↖ / СОРТІВ ↘), пляшка видима в центрі між словами.
- Пляшка губилась → повернуто оригінальне `hero-bottle.png` (видима **біла етикетка**), освітлено (brightness .78), легша віньєтка.
- «жодного зайвого» → **тонка вага** Unbounded 300 (контраст до важких слів).
- Баг: бігуча стрічка не рухалась — `overwrite:true` на skew вбивав нескінченний x-tween. Skew перенесено на `.marquee` (окремий таргет) + `overwrite:"auto"`; підтверджено рух (translateX змінюється).

### 2026-07-01 — РЕДИЗАЙН v2 за новим стандартом 02-design (reference-driven)
- Причина: оновлені жорсткі правила `02-design.md` (reference-driven, бан спліт-hero, бан Oswald/Manrope-як-дисплей, шрифти з award-джерела=CSS, GSAP+Lenis, кастомний курсор + прелоадер, формула промтів).
- Перечитано заново: CLAUDE.md, 02-design.md, 01-analysis.md, brief/analysis/DESIGN/status.
- **Референси** (`resources/refs/references.md`): Bunta Beer (pin+scrub product), Angle Brewery (horizontal-pan колекція), Independent Brewers of Europe (kinetic scrollytelling), GRITZ (тип-як-герой), Onyx (моно мета-мітки). Синтез, не копія.
- **Design Read:** cinematic-dark editorial + brutal giant-type; дайли V9/M9/D3.
- **Шрифти (важливо):** award-фаворити Clash Display/Satoshi/Space Mono = **Latin-only**, падали у фолбек на укр. → відкинуто. Обрано **Unbounded + Onest + JetBrains Mono** (Google, повна українська), DESIGN.md збігається з CSS/link.
- **Hero:** гігантський kinetic-тип по центру (Unbounded, char-reveal), пляшка приглушена як кінематографічне тло + vignette. НЕ спліт-колонки.
- **Секції/архетипи (різні):** прелоадер-інтро з лічильником (=age-gate) → hero → kinetic marquee (velocity-skew) → **pinned scrollytelling** маніфест (світла інверсія) → **горизонтальний пан 6 сортів** (pinned, index-watermark) → pinned pour-moment (parallax) → broken-staggered craft → асиметричне замовлення (кошик+форма) → FAQ → гігантський футер.
- **Моушн:** GSAP 3.13 + Lenis (CDN), pin/scrub/horizontal-pan/kinetic/parallax/magnetic. Reduced-motion: Lenis off, pins off, курсор off, інтро миттєве.
- **Дрібниці:** кастомний курсор (dot+ring, магнітиться до `[data-magnetic]`) + прелоадер. Палітра ЧБ + стриманий амбер `--signal` (курсор/мітки/active).
- **Фікси під час верстки:** шрифт-фолбек (кирилиця); hero-overflow (широкий Unbounded) → слова стосом + менший підрядок; панель-overflow → index-watermark; order-grid overflow → minmax(0,…).
- Верифікація: прев'ю 5810, десктоп 1280 + мобільний 390 (горизонт. пан → вертикальний стек), кошик/форма/акордеон/інтро/курсор ок, консоль чиста, em-dash лише в коментарях.
- Файли: `resources/refs/references.md`, `resources/refs/image-prompts.md`, оновлені `DESIGN.md`, `design/{index.html,styles.css,main.js}`, нові фото в `design/assets/`.

### 2026-07-01 — дизайн: прототип головної (02)
- Що зроблено: `DESIGN.md` (mono-токени) + повний прототип у `design/` (`index.html`, `styles.css`, `main.js`).
- Дайли: VARIANCE 7 / MOTION 7 / DENSITY 3. Палітра mono-lock (#0A0A0A / #FBFBF9, без хроматичного акценту). Shape lock: гострі кути.
- Секції: age-gate 18+ → hero (пляшка-герой, GSAP паралакс) → marquee → manifesto «Шість. Не більше.» → 6 сортів (editorial-меню 01–06) → крафт (ЧБ-фото) → форма замовлення з кошиком → FAQ-акордеон → футер.
- Медіа (M2): 4 AI-фото через Pollinations у `design/assets/` (hero-bottle вийшов відмінно). Позначені TODO до реальних фото.
- Рішення/фікси:
  - Display-шрифт **Oswald** (НЕ Anton — Anton без кирилиці, падав у serif). Body — Manrope.
  - Прибрано всі em-dash з видимого тексту (вимога taste-скіла).
  - Маніфест переписано зі «стіни» гігантського тексту на коротке гасло + body.
  - Мобільний hero підтиснуто, щоб оффер+CTA трималися екрана.
- Верифікація: прев'ю (порт 5810), десктоп 1280 + мобільний 375, кошик/форма/акордеон працюють, консоль чиста.
- Наступний крок: фідбек користувача.

### 2026-07-01 — аналіз завершено (01)
- Що зроблено: `resources/analysis.md` — 2 треки джерел (лідери ніші + дизайн-бенчмарки), синтез, ICE, структура головної, напрям дизайну.
- Ключові орієнтири: **MOVA** (ecommerce-flow, Druk+Neue Haas, 3D-пляшки, mobile-first замовлення), **Basei** (ЧБ + 1 акцент), **Stadshaven** (чорний фон + bold тип), **Curators of Craft** (ЧБ-сцена під продукт, фільтр стиль/ABV). Анти-реф: **Beavertown** (строкатий мультяшний — НЕ йдемо).
- Концепція: **editorial-мінімалізм ЧБ + «рівно 6 сортів» (нумерація 01–06)**, пляшка-герой зі scroll-моушном (GSAP), повітря як преміум-сигнал.
- Структура (8 секцій): age-gate → hero → філософія «6» → 6 сортів → процес → форма-замовлення → FAQ/доставка → футер.
- Наступний крок: `02-design`.

### 2026-07-01 — старт, intake завершено
- Що зроблено: заведено теку `projects/bropicer/`, заповнено `brief.md` і `status.md`.
- Рішення (і чому):
  - **B0** (бренд з нуля) — старого бренду нема; будуємо чорно-білу систему під фізику продукту.
  - **M2** (AI-генерація) — реальних фото нема; генеруємо студійні чорні пляшки, локально в `design/assets/`, як позначені плейсхолдери.
  - **Ціль = замовлення онлайн** — головна метрика конверсії; форма замовлення на фронті, процесинг TBD.
  - Стек на старті — vanilla прототип у `design/`.
- Дизайн-код продукту: **чорні-чорні пляшки + біла кришка + біла етикетка**, лінійка = **6 сортів**. Це ядро візуалу.
- Наступний крок: `01-analysis` — аналіз конкурентів, збір референсів, фіксація напряму дизану в `DESIGN.md`.

## Ключові рішення (швидкий доступ)
- Бренд: B0 (з нуля), естетика — мінімалізм чорний/білий.
- Медіа: M2 (AI-генерація пляшок), ассети локально.
- Ціль: замовлення онлайн (односторінковий лендинг).
- УТП: рівно 6 сортів; чорна пляшка / біла кришка / біла етикетка.

## Корисні посилання
- Прод: TBD
- Staging: TBD
- Репозиторій: TBD
